(define-foreign (log ()) (n i32))

(struct Point
	(x i32)
	(y i32))

(define (log-point ()) (p Point)
	(log (field p x))
	(log (field p y)))

(define (main i32) ()
	(begin
		(let p (Point 3 2))
		(log-point p)
		0))
